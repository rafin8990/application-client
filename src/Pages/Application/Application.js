import React from 'react';
import Navbar from '../../Navbar/Navbar';

const Application = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default Application;